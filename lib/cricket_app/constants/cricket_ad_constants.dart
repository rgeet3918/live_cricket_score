/// Cricket App Ad size constants.
/// Use these everywhere for banner/native ad height so one change updates all places.
class CricketAdConstants {
  CricketAdConstants._();

  /// Height used for native ad widget and ad placeholder containers (banner-style).
  /// Used in: home, schedule, all matches, match details screens, cricket_ad_service default.
  static const double adHeight = 250;
}
